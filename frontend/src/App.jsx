// Canonical application entry point. The role-aware implementation lives in
// AppUpdated.jsx so the original presentation modules remain easy to inspect.
export { default } from "./AppUpdated.jsx";
